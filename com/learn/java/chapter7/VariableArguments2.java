package com.learn.java.chapter7;

public class VariableArguments2 {
	/**
	 * method(int i,byte j)
	 * method(byte ... i)
	 * 
	 * when we can method with parameters like 
	 * method(byte i,byte j) it will call the line 5 (first method) because it first checks the number
	 * of parameters and then checks the data type and do type casting if required
	 */
}
